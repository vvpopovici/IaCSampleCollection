name: $(Date:yyyyMMdd)-$(rev:rr)-DEV-$(SourceBranchName) # == $(Build.BuildNumber)

trigger: none

stages:

  - stage: Build
    jobs:
      - job: Build
        steps:
          - script: |
              echo "Build the code..."
              echo "Build the code..." > $(Build.ArtifactStagingDirectory)/build_output.txt
          - publish: $(Build.ArtifactStagingDirectory)
            artifact: drop

  - stage: Deploy_To_DEV
    jobs:
      - job: Deploy_To_DEV
        steps:
          - download: current
          - script: echo "Deploy to Development environment..."
